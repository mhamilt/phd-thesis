%----------------------------------------------------------------------------------------
% Document Setup and Geometry
\documentclass[11pt,a4paper,openright,twoside]{book}
\usepackage[
     paper = a4paper, % 21 x 29.7 cm
       top = 2.10cm,  
    bottom = 2.60cm,  
      left = 3.25cm,  
     right = 3.25cm,  
     % inner = 4.00cm,  % Two sided options for binding
     % outer = 2.50cm,  % Two sided options for binding
     %  twoside,        % Two sided options for binding
includehead,        
includefoot         
]{geometry}
%----------------------------------------------------------------------------------------
% Packag options
\PassOptionsToPackage{T1}{fontenc}
\PassOptionsToPackage{utf8}{inputenc}

\usepackage{
    adjustbox,
     amsfonts,
    afterpage,
      amsmath,
      amssymb,
      amstext,
     booktabs,
      caption,
   changepage,
   circuitikz,
        color,
     enumitem,
     eqparbox,
      environ,
     fancyhdr,
     fancyvrb,
        float,
      fontenc,
     graphicx,
  indentfirst,
     inputenc,
     listings,
     multicol,
     multirow,
    newtxtext,
    newtxmath,
    pmboxdraw,
     setspace,
   subcaption,
          svg,   
   tabularray,
        xcolor
}

\usepackage[hyphens]{url}
\usepackage[hidelinks]{hyperref}
%----------------------------------------------------------------------------------------
% Table and Matrix Vertical Space
\renewcommand{\arraystretch}{1.4}
%----------------------------------------------------------------------------------------
\usepackage{newunicodechar}
\newunicodechar{└}{\textSFii}
\newunicodechar{┴}{\textSFvii}
\newunicodechar{┬}{\textSFvi}
\newunicodechar{├}{\textSFviii}
\newunicodechar{─}{\textSFx}
\newunicodechar{┼}{\textSFv}
\newunicodechar{│}{\textSFxi}
% \usepackage{breakurl}
%----------------------------------------------------------------------------------------
\usepackage{tikz}
\usepackage{forest}
\usetikzlibrary{arrows, positioning, shapes.symbols, shapes.callouts, patterns, calc}
\newcommand\rowsep{3.6}
\UseTblrLibrary{amsmath}
\setcounter{MaxMatrixCols}{25}
\usetikzlibrary{calc}
%----------------------------------------------------------------------------------------
% Bibliography
\usepackage[
        style = numeric-comp,
      backend = biber,
     maxnames = 1,
 maxcitenames = 1,
   uniquelist = false,
          doi = false,
          url = true,
      sorting = nyt,
    sortcites = true,    
]{biblatex} 
\addbibresource{main.bib}

%% Possessive Citation
\DeclareNameFormat{poss}{%%
  \nameparts{#1}%%
  \ifnum\value{listcount}=1%
    \ifnum\value{liststop}=1%
      % single author: add 's%
      \usebibmacro{name:family}{\namepartfamily}{\namepartgiven}{\namepartprefix}{\namepartsuffix}'s%%
    \else%
      % multiple authors: no 's yet%
      \usebibmacro{name:family}{\namepartfamily}{\namepartgiven}{\namepartprefix}{\namepartsuffix}%%
    \fi%
  \else%
    \ifnum\value{listcount}<\value{liststop}%
      {, }%%
    \else%
      % last author in multiple list: add 's%
      \usebibmacro{name:last}{\namepartfamily}{\namepartgiven}{\namepartprefix}{\namepartsuffix}'s%%
    \fi%
  \fi%
}%

\DeclareCiteCommand{\citep}[] % keep prenote/postnote machinery%
  {\usebibmacro{prenote}}%
  {%%
    % Author name(s) in possessive form%
    \printnames[poss]{author}%%
    \setunit{\space}%%
    % Now the numeric citation%
    \printtext{[\printfield{labelnumber}]}%%
  }%
  {\multicitedelim}%
  {\usebibmacro{postnote}%
}%

%----------------------------------------------------------------------------------------

\makeatletter
\newcommand{\SAVED@cleardoublepage}{} 
\let\SAVED@cleardoublepage\cleardoublepage
\renewcommand*{\cleardoublepage}{
  \clearpage 
  \begingroup 
    \pagestyle{empty}
    \SAVED@cleardoublepage 
  \endgroup 
} 
\makeatother
\hyphenpenalty=5000 % helps to discourage hyphenation 10000 for no hyphens
\tolerance=1000

%%%%%%%%%% DO NOT EDIT %%%%%%%%%%

%----------------------------------------------------------------------------------------
% Footnote Formatting
\usepackage{xparse}
\usepackage{expl3}

\ExplSyntaxOn

% Save original \footnote
\cs_new_eq:NN \origfootnote \footnote

% Define our safe version
\RenewDocumentCommand \footnote { m }
  {
    % delete space but give a warning
    \ifhmode
      \ifdim\lastskip>0pt
        \unskip % delete the preceding space    
    \fi

      %   \PackageWarning{footnote-check}
      %     {Removed\space a\space space\space immediately\space before\space \string\footnote}
      % \fi

    % Insert the real footnote
    \origfootnote{#1}\space

    % --- check for punctuation immediately after ---
    \peek_meaning:NTF . { \__fn_bad_punct: } {
    \peek_meaning:NTF , { \__fn_bad_punct: } {
    \peek_meaning:NTF ; { \__fn_bad_punct: } {
    \peek_meaning:NTF : { \__fn_bad_punct: } {
    \peek_meaning:NTF ? { \__fn_bad_punct: } {
    \peek_meaning:NTF ! { \__fn_bad_punct: } {
    \peek_meaning:NTF ) { \__fn_bad_punct: } {
    \peek_meaning:NTF ] { \__fn_bad_punct: } {
    \peek_meaning:NTF } { \__fn_bad_punct: } {
      % no punctuation immediately after -> OK
    }}}}}}}}    
  }

% Helper macro for bad punctuation
\cs_new_protected:Npn \__fn_bad_punct:
  {
    \PackageError{footnote-check}
    {Footnote\space immediately\space followed\space by\space punctuation}
    {Remove\space the\space punctuation\space or\space place\space it\space differently.}
  }

\ExplSyntaxOff
%----------------------------------------------------------------------------------------
