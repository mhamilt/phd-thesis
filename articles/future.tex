
\section{Introduction}
    
The projects covered in the previous chapters have use cases and application in their current form, however the  work here represents not a terminus of the ideas but rather a rich possibility for expansion.
By refining and developing the work covered herein, these should hopefully be worthwhile tools within research to come. This chapter covers the areas for development within each project and impact that may have on future research.

\section{Interface}

\begin{figure}
    \centering
    \includegraphics[width=0.7\linewidth]{example-image}
    \caption{PLACEHOLDER for}
    \label{fig:placeholder}
\end{figure}


\subsection{As Research Probe}

One of the richest avenues for further research using the interface is as part of a larger study that explores interface haptic feedback as well as the potency of visual aesthetics to player experience.

Such studies previously mentioned have already covered the harpsichord.

For haptics, A double blind study involving a similar installation as \textcite{mcalpine_sampling_2014} as well as a full scale instrument
It should first be determined if mechanical recreation is sufficient as part of a digital musical instrument before pursuing elaborate electromechanical emulation.
Does recreating the harpsichord action at a smaller scale achieve tactile realism? Answer to that question would provide grounds for further work on a purely actuated keyboard.

The ability for the jacks to be disengaged and prevent plucking the string, but still capable of triggering a MIDI message is ripe for exploration. The Mark 2 interface will hopefully serve as a research probe to explore the unique characteristics of the harpsichord and its impact on performance, much in the same manner as other musical haptics studies \cite{charalampos-saitis_musical_2018}.

\subsection{Study of Visitor Feedback}


\subsection{Research Software and Academic Institutes}


\subsection{Refinement}

\begin{itemize}
    \item could be made smaller, but would be sensible to explore if the tactile elements make a difference
    \item could use cad cam parts, stickers aren't great
    \item fabrication could be parametarised for jack spacings
    \item fabrication be largely automated, no need for THT parts. SMD would shrink things massively.
    \item improved circuitry, incorporating a similar op amp circuit from \cite{mcpherson_portable_2013} which can allow for a tuned voltage range, utilising the full range of the ADC used rather than what is given.
\end{itemize}


A separate sensor module, splitting the current sensor board into a board containig just the sensor which is then routed to an expanded controller board.

A similar conept can be seen in \textcite{schmidt_sparksichord_2025}, where optical sensors at a fixed pitch are connected to headers to allow for a more modular assembly. This would help to overcome implementation in a constrained space. The footprint of the pcb being reduced where the vertical span wil no longer be a problem, thus avoiding problems such as blocking the keys.

One approach would be to change the microcontroller. A test
was carried out using an Arduino Nano ESP32 which is a NORA-W106, a module containing an ESP32-S3. 
The benefit of this microcontroller is that it would allow the microcontroller to be swapped and the rest of the PCB
designs would be unaffected.

Another approach would be to use a different microcontroller entirely. 
Some research was carried during the course of the project using an ESP32 programmed with the Espressif development framework ESP-IDF. 
The same logic as the original firmware was programmed and the latency was reduced to below 10 ms. ESP-IDF allows access to functionality of the ESP32 including regular processing of an internal audio buffer that is not exposed in the Arduino IDE. 
It is possible that the 10 ms latency could be improved upon with appropriate optimisation, but there would be a cost in the form of development time. 
Consideration would need to be made on the time to port existing source code for the I2C FRAM chip or fall back on a simpler storage option.

Use of QRD1114 which has a faster rise time, greater sensitivity. Rest of the deisgn could be keptthe same and the QRD1114 dropped in place of the QRE1113. This is at the cost of greater power consumption (around 0.1V per sensor, but current would also change)

\url{https://www.onsemi.com/pdf/datasheet/qrd1114-d.pdf}
\url{https://www.onsemi.com/download/data-sheet/pdf/qre1113-d.pdf}


A move from THT to SMD components would be expected to reduce the footprint of the PCB by half \cite{prasad_surface_1997} allowing the space to be used for a refined op-amp design \cite{mcpherson_portable_2013} or the ability to install in more constrained spaces.

The system would benefit from single piece mounting bracket which would allow for much more simplified installation. Given the open source nature of the project the advantages come from the ability to modify the design for a bespoke application.

\subsection{Site Specific Enhancements}


adding shakers to seat
convolving with impulse response from point of audition (IF headphones)



\subsection{CAD/CAM processes}
Easing acces to fabrication and permitting iteration on designs


\subsection{Perpetual Software Refinement}

\section{MAGPIE}

Improvements to \textsc{magpie} can be split between those of functionality and those of interface. The core functions of \textsc{magpie} would already provide support for number of research use cases, but further development is needed to enable it as a powerful open source tool for musical acoustics. Similarly, there is functionality that exists but a efficient interface is needed in order to full utilise it. \textsc{magpie} seeks to turn traditionally physics based problems into a problems of engineering and interface is key in order to facilitate that.

Development within \textsc{MAGPIE} can be focussed within two directions. One in functionality and the other in portability. The current iteration is a capable illustrative tool for educational purposes and some specific material measurement scenarios. The application for tone wood still needs to be develped further. Similiarly, the tool is available now in the open source language Python, but the current interface relies on the Jupyter notebook system, which comes with its own technical and portability challenges.

Key area for expansion is to translate the source code to a compiled language such as \textsc{C}, \textsc{C++}, and Rust. Such a transition provides more granular control of compiler optimisations, but more interestingly opens potential for deployment. 

The primary bottleneck for development is the eigensolver stage. In MATLAB this achieved with \texttt{eigs} and in python the SciPy linear algebra library's \texttt{scipy.sparse.linalg.eigs}. In both cases, these eigensolvers are an interface to the ARPACK library \cite{lehoucq_arpack_1998}. For MATLAB there is analysis of the target matrix to which the user is not privvy. 
MATLAB its own sparse library on top of ARPACK \cite{gilbert_sparse_1992} which obfuscates the process further.
MATLAB's \texttt{eigs} function is not one supported by the software's code generation systems and the function still requires the MATLAB runtime if it is to be interfaced with externally. This has implications for portability issues, with development being constrained to those with paid access to the MATLAB environment.

ARPACK itself is open source \url{https://github.com/opencollab/arpack-ng} and requires The Basic Linear Algebra Subroutines (BLAS) and Linear Algebra PACKage (LAPACK) \url{http://www.netlib.org/blas/} and \url{http://www.netlib.org/lapack/}

In the case of \textsc{C++}, the relevant technologies available for interfacing with ARPACK library. First is the open source Eigen library for C++ \url{https://gitlab.com/libeigen/eigen} and the Spectra library \url{https://spectralib.org} for eigenvalue problems. Spectra consists of a re-write of ARPACK solvers in C++ allowing for direct native compilation. 



\subsection{Functionality}

\subsubsection{Tuning}

Taking \textcite[]{duran_experimentally_2023} as an example.

\begin{table}[]
	\centering
	\begin{tabular}{l r r r r r r}
		                    & 1  & 2   & 3   & 4   & 5   & 6   \\ \hline
		\textbf{Clamped}    & 77 & 153 & 382 & 486 & 596 & 880 \\
		\textbf{Experiment} & 73 & 148 & 376 & 431 & 559 & 910 \\
		\textbf{Adjusted}   & 75 & 150 & 370 & 458 & 557 & 844 \\
	\end{tabular}
	\caption{Eigenfrequencies for experiment data in \cite{duran_experimentally_2023} against a fully clamped and adjusted boundary conditions using \textsc{magpie}}
	\label{tab:tuning}
\end{table}


\begin{figure}
    \centering
    \includegraphics[width=0.5\linewidth]{img/magpie-tuning.pdf}
    \caption{Example interface visual feedback for `tuning' boundary conditions and simulation parameters to match known experimental data.}
    \label{fig:magpie-tuning}
\end{figure}


\subsubsection{Orthotropic}


\cite{ducceschi_method_2024}

as part of that grid spacing separate for x and y directions. Currently the system is optimum for plates that are integer multiple aspect ratios. When plates fall outside of this, such as in \textcite{duran_experimentally_2023}, then accuracy suffers.

\subsubsection{Bracing}

\cite{ducceschi_method_2024}

\subsubsection{Optimisation}

Further refine the \textsc{c++} and python versions. Relationship between sparse format and eigen solver speed is not clear what the optimum options are for the specific case of the biharmoinc.

Given the ultra specific case of the biharmonic, may not require generalised solvers and a more bespoke solving solution may become apparent.

\subsection{Interface}

\subsubsection{C++ Interface}

- juce, open frameworks


\subsubsection{Web Technology}

Porting \textsc{magpie} to a compiled language like \textsc{C++} open up a number of exciting possibliities. 
Similarity in results to that of \textsc{matlab} give an indication of a viable plate analysis tool that is not constrained to only those system on which \textsc{matlab} is supported. Of particular  note is the possibility of deploying \textsc{magpie} as an entirely web based application. Traditional software has typically required some form of installation process, where binaries are compiled for a specific machine architecture. Web technologies do away with part of that necessity by delivering software directly through a browser.
One such technology is Web Assembly (WASM) \url{https://webassembly.org}, which allows for optimised binary code to be executed by a virtual machine in the browser. This is achieved through the emscripten,\footnote{\url{https://emscripten.org}} an open source compiler that enables compilation of \textsc{C} / \textsc{C++} code into WASM. Assembly code capable of more performative execution than the equivalent JavaScript 
\cite{reiser_accelerate_2017}. When combined with the native \textsc{C++} source of Eigen and Spectra, the emscripten compiler can potentially be used to distribute and execute \textsc{magpie} entirely in the browser.
\footnote{Such an application using the Eigen library has already been demonstrated as feasible \url{https://observablehq.com/@rreusser/eigen}}. 
To make a web tool like \textsc{magpie} would traditionally require server architecture in order to execute ARPACK or to host a MATLAB runtime. 
In a server / client arrangement the bottlenecks on performance are then tied to the slowest of the network and the computation.
Creating a \textsc{magpie} server would also not be without cost and would also be limited by concurrent usage.
The server and client can be on the same machine, which is exactly the architecture for the Jupyter interface (Section~\ref{sec:magpie-jupyter}).
The extra technical requirements for installation of a local server reintroduces platform specific behaviour and limitations, which the browser client is meant to avoid.
Computational performance in this scenario is also affected compared to an equivalent system using natively compiled binaries.
The alternative for \textsc{magpie} to execute entirely on the client-side would be an implementation of a sparse matrix library and eigensolver entirely within JavaScript, the result of which would be severely limited in performance \cite{lion_investigating_2022}.
\textsc{magpie} would likely see much more utility if development is pursued for an entirely native or entirely web client approach.
The usage of WASM would allow for compilation for both approaches from a single, shared codebase.
It is worth noting that the above web technologies are not supported equally across all browsers and their versions.\footnote{\url{https://developer.mozilla.org/en-US/docs/WebAssembly\#browser_compatibility}}
Such limitation of highlight an importance of both open sourcing and version control as multiple systems and deployments can more easily be supported.

WASM addresses the porting of the core \textsc{magpie} to the web browser, but there still remains a question o of interface.
The Jupyter version of \textsc{magpie} (Section~\ref{sec:magpie-jupyter}) demonstrates one potential interface, but this is limited by the communication happening between IPython the renderer for the browser. Breaking away from the constraint of Jupyter allows for direct usage of further browser technology, notably the WebGL and WebGPU \cite{chickerur_webgl_2024} which allow fast 3D drawing within the browser. Figure~\ref{fig:magpie-p5js} shows a prototype example of the first four eigenshapes of a plate above their corresponding `chladni pattern' projected shadows.\footnote{\url{https://editor.p5js.org/mhamilt/full/WerJc4OJM}} The plates in Figure~\ref{fig:magpie-p5js} are drawn using the \texttt{p5.js} library which has an interface to WebGL.
\footnote{\url{https://p5js.org/reference/p5/WEBGL/}}
Each plate in comprised of approximately 1.5k grid points, for a total 12k gridpoints across displacement and projected shadow and running at around 11 frames per second (FPS). The interface is an early proof of concept, using eigenvectors from the \textsc{c++} version of \textsc{magpie} output as javascript arrays. Although the plates are rendered at a low FPS, this would likely be improved by implementing displacement and shading as part of vertex and fragment shaders rather than direct drawing using \texttt{p5.js}.

\begin{figure}
    \centering
    \includegraphics[width=0.5\linewidth]{img/magpie-p5js.png}
    \caption{Prototype interface using WebGL via p5.js to render plate modes}
    \label{fig:magpie-p5js}
\end{figure}


\paragraph{ITEMM OpenLab / Existing Tools}

\textsc{magpie} is by no means the first tool to provide the functionality described here and neither is it the only ``free'' tool available to do so. The Institut Technologique Européen des Métiers de la Musique (ITEMM) provides a suite of tools for luthiers via its online service known as ITEMM Open Lab\footnote{\url{https://www.openlab-itemm.com}}. ITEMM OpenLab provides resources for thos in the field of musical instrument making, hosting datasets of materials, 3D models and CAD files for instrument parts and tools. Most relevent, is that ITEMM OpenLab also provides a suit of tools for calculating wood properties intended to provide luthiers accesss to toools and processes used in the ITEMM Laboratories.
% Notre objectif est de rendre accessibles des méthodes scientifiques développées en laboratoire, en les adaptant à des moyens simples et peu onéreux utilisables directement dans un atelier de lutherie.
% 
Available tools include those for calculating, resonance frequencies of soundboards and easels, string simulation strings under tension, and wood properties. The \textit{Rigidité de plaques} tool provides a means calculating the elastic constants of wood, .\footnote{\url{https://www.openlab-itemm.com/calculateur-mecanique/rigidite-de-plaques-1/}} using the methodology laid out by \textcite{mcintyre_measuring_1988}. 
The open availability of the tools at ITEMM OpenLab are adminrable, but there are some inherent restrictions.
The \textit{Rigidité de plaques}, like the other calculators on OpenLab, is free to use however the tools themselves are accessible only after registering for the service and authenticating. 
The ``free as in freedom'' distinction made by \textcite{stallman_why_2007} would not classify \textit{Rigidité de plaques} as a FLOSS tool. 
The source is ``open source'' in the sense that, given it is software delivered via a web browser, the source code is downloaded onto the user's computer, but it is not easily identified nor is it documented.
ITEMM OpenLab meets a technical definition of free open source software, but sadly one where the free and open are lowercase.
One can, after time, identify the relevant files,
\footnote{calculations take pace in \url{https://www.openlab-itemm.com/wp-content/plugins/ITEMM-OpenLab-tools-main/js/p_w_script.js} and an \texttt{olt} utility class \url{https://www.openlab-itemm.com/wp-content/plugins/ITEMM-OpenLab-tools-main/js/olt_class.js} used across calculators, both of which available without authentication.} 
but they are missing documentation, attribution and an open source licence. 
The legal notice\footnote{\url{https://www.openlab-itemm.com/mentions-legals/}} on ITEMM OpenLab covers the 3D objects, but this does not naturally extend to the software \cite{cc_can_2025}. Instead, the software calculators should have an explicit open source licence attached to them so that there terms of use are clearer. From ITEMM OpenLab's desciprtive text and legal notice it is clear that attribution and accessibility are core principles, both of which would be better served by a clear citation system \cite{soito_citations_2016}, especially if these tools are to be used for further use.
The 'open first' design methodology of \textsc{magpie} was in part to avoid issues in lack of clarity for reuse.
\textcite{stallman_floss_2021} differentiates between free open source software (FOSS) and free / libre open source  software (FLOSS). Such a distinction is to make clear the meaning of `free' given its ambiguity of free as in ``free speech'' or as ``free beer'' \cite{stallman_why_2007}. FLOSS is a distinction of open source software where ``free'' carries both meanings.
Web tools for plate analysis may be extant, but there is still a gap for a truly free and open tool where the `free' and `open' are not lowercase.

\subsubsection{P5.js}



\begin{figure}
    \centering
    % \includegraphics[width=0.5\linewidth]{}
    \caption{Parameters for the Biharmonic used in benchmarking tests/}
    \label{fig:eigensolver-performance}
\end{figure}


\begin{figure}
    \centering
    \includegraphics[width=0.5\linewidth]{img/plot-biharmonic-comparison.pdf}
    \caption{Compute time of for the biharmonic from 1000 to 1000000 grid points in MATLAB, Python and C++.}
    \label{fig:bihamronic-performance}
\end{figure}


\begin{figure}
    \centering
    \includegraphics[width=0.5\linewidth]{img/plot-eigensolver-comparison.pdf}
    \caption{Compute time for 10 to 500 eigenvalues using an eigensolver in shift-invert mode in MATLAB, Python and C++. Biharmonic consisting of 10000 grid points with parameters given in Figure~\ref{fig:benchmark_parameters}.}
    \label{fig:eigensolver-performance}
\end{figure}


For Figure~\ref{fig:bihamronic-performance} the computation was run over 20 iterations and the fastest time was then selected. For \textsc{C++} the compute times for \textsc{-O3} and \textsc{-Ofast} optimisations settings are shown.


% Model Name:	MacBook Pro
% Model Identifier:	Mac14,9
% Model Number:	MPHH3T/A
% Chip:	Apple M2 Pro
% Total Number of Cores:	10 (6 performance and 4 efficiency)
% Memory:	16 GB


MATLAB has functionality to compile its code to WASM (\url{https://www.mathworks.com/matlabcentral/fileexchange/69973-generatejavascriptusingmatlabcoder}). Some functionality such as that in MATLAB's sparse libraries is not currently open to code generation and compilation to a binary outside of the MATLAB runtime (list of functions supported: \url{https://uk.mathworks.com/help/referencelist.html?type=function&capability=codegen&listtype=alpha}) and it is unlikely to be in the near future.


A key development for \textsc{MAGPIE} is the addition of an orthotropic model so match more closely that of the quarter-sawn wood as is used for string instrument soundboards. Even further, the addition of braces and lumped masses to the internal system to mimic that of the structural bracing found on physical instruments \cite{ducceschi_open_2024}. 

Presenting the tool kit in an open source well-structure manner should hopefully encourage usage, collaboration and development as the system matures.

\subsubsection{Emebedded Systems}

As part of a lutthier toolkit, more than capable of running on a Bela.

avoiding a tivoized device \url{https://www.gnu.org/philosophy/free-open-overlap.html}

